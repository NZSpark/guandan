# Aotearoa 掼蛋俱乐部排位系统 — 编程学习大纲

> 项目地址: `d:/Github/guandan` · GitHub: `github.com/NZSpark/guandan`
>
> 技术栈: ReScript 11 + React 19 + Vite 7 + IndexedDB (localforage) + Python

---

## 第一章 · 项目总览与技术栈认知

### 1.1 项目是什么
- 一个基于 **瑞士移位制** 的掼蛋赛事管理 SPA（单页应用）
- 支持选手管理、队伍配对、赛制编排（瑞士制 / 小组赛 / 淘汰赛）、积分排名
- 纯前端，无需服务器，数据存于浏览器 IndexedDB

### 1.2 核心技术栈一览

| 层级 | 技术 | 用途 |
|------|------|------|
| 语言 | **ReScript 11** | 强类型编译到 JS 的函数式语言 |
| UI | **React 19** | 组件化 UI |
| 构建 | **Vite 7** | 开发服务器 + 生产打包 |
| 存储 | **localforage** (IndexedDB) | 浏览器端持久化 |
| 算法 | **rescript-blossom** | 图的完美匹配（Blossom 算法） |
| 测试 | **Vitest 3** | 单元测试 |
| 包管理 | **pnpm** | monorepo 友好的包管理器 |
| 辅助 | **Python** | 模拟器 / 参考实现 |

### 1.3 为什么要学这个项目
- **类型安全**：ReScript 的代数类型系统远超 TypeScript，零运行时开销
- **函数式编程**：immutable 数据、管道操作、模式匹配贯穿始终
- **真实工程**：不是 TODO app，是真实上线的生产级应用
- **算法深度**：图论配对、排序、分组、淘汰赛 bracket 模板

---

## 第二章 · ReScript 语言核心

### 2.1 基础语法
- **变量绑定**：`let x = 42`（不可变）、`let mutable x = ref(0)`（可变引用）
- **类型标注**：`let x: int = 42`
- **类型别名**：`type myInt = int`
- **类型参数**：`type t<'a> = array<'a>`

### 2.2 代数数据类型 (Variant / ADT)
这是 ReScript **最重要的特性**，项目中几乎每个文件都在用：

```rescript
// 简单枚举
type direction = Ascending | Descending

// 携带数据的 variant（项目核心模式）
type getter<'a> =
  | GetString('a => string)
  | GetInt('a => int)
  | GetFloat('a => float)
  | GetDate('a => Js.Date.t)

// 递归 variant
type t = 
  | Status | Setup | Players 
  | Round(int) | Scores(int) | Bracket
```

**项目中的实践**：
- `Data_Level.t`（13 个扑克级数 variant）
- `Tournament.Format.t`（3 种赛制）
- `Router.t`（URL 路由映射）
- `TieBreak.t`（6 种破同分规则）

### 2.3 模式匹配 (Pattern Matching)
```rescript
// 穷举匹配 — 漏掉一个 variant 编译器就报错
switch activeTab {
| Status => true
| Setup | Players | Round(_) | Scores(_) | Bracket => false
}

// 嵌套匹配
switch match.result.winner {
| Some(Team1Won) => isTeam1 ? 1.0 : 0.0
| Some(Team2Won) => isTeam1 ? 0.0 : 1.0
| None => 0.5  // 平局
}
```

### 2.4 Record 类型（不可变记录）
```rescript
type teamScores = {
  id: Data_Id.t,
  fieldScore: float,
  opponentScore: float,
  wins: int,
  netSmallScore: int,
  cumulativeSmallScore: int,
}

// 更新：展开语法（创建新记录，不修改原记录）
{...state, avoidClubs: true}
```

### 2.5 Option 类型（取代 null）
```rescript
// 安全地处理"可能没有值"
let round = Rounds.get(roundList, roundId)
switch round {
| Some(r) => /* 有数据 */
| None => /* 没数据 */
}

// 链式操作
value->Js.Nullable.toOption->Option.map(decode)
```

### 2.6 Pipe 操作符 `->`
```rescript
// 数据从左到右流动，取代嵌套函数调用
roundList
->Rounds.rounds2Matches
->Array.reduce(Id.Map.make(), (acc, match) => ...)
```

### 2.7 Functor（高阶模块）
```rescript
// Db.res 的核心设计模式
module type Encodable = {
  type t
  let encode: t => Js.Json.t
  let decode: Js.Json.t => t
}
type encodable<'a> = module(Encodable with type t = 'a)

// 使用
Store.Map.make(config, module(Data.Player))
```

### 2.8 First-class Module 与 `unpack`
```rescript
// 将模块包装为值
let data: encodable<'a> = module(Data.Player)

// 拆包：值 → 模块
module Data = unpack(data)
let encoded = Data.encode(value)
```

### 2.9 外部 JS 绑定
```rescript
// @module — 从 npm 包导入
@module("localforage") @scope("default")
external createInstance: config => localforage = "createInstance"

// @send — JS 方法调用
@send external setItem: (localforage, string, Js.Json.t) => Promise.t<unit> = "setItem"

// @val — 全局变量
@val external isTest: bool = "__IS_TEST__"

// @obj — 声明 JS 对象字面量
@obj external config: (~name: string, ~storeName: string, unit) => config = ""

// %raw — 嵌入原始 JS
%raw(`document.body.style.cursor = "wait"`)
```

---

## 第三章 · 类型系统的深度应用

### 3.1 自定义 ID 与比较器
```rescript
// 用 Belt.Id.comparable 创建可用于 Map/Set 的自定义类型
module Id = {
  type t = string
  let compare = (a, b) => String.compare(a, b)
}

module Map = {
  type t<'a> = Belt.Map.t<Id.t, 'a, Id.comparable>
  let make = () => Belt.Map.make(~id=module(Id))
}

module Set = {
  type t = Belt.Set.t<Id.t, Id.comparable>
  let make = () => Belt.Set.make(~id=module(Id))
}
```

### 3.2 有序对 (Ordered Pair)
```rescript
// Pair 用于表示无序二元组（(A,B) == (B,A)）
// 用于"避免匹配"集合
module Pair = {
  type t = (Id.t, Id.t)  // 自动排序存储
  module Set = Belt.Set  // Pair 的集合
  let toMap = ...  // 扁平化为 Id→Id.Set 映射
}
```

### 3.3 Opaque 类型（隐藏实现细节）
```rescript
// Data_Rounds.resi — 接口文件
type t  // 对外不暴露内部结构
let size: t => int
let addRound: t => t
let get: (t, int) => option<Round.t>

// Data_Rounds.res — 实现文件
type t = array<Round.t>  // 内部是普通数组
```

### 3.4 `@deriving(accessors)` 自动生成 getter
```rescript
@deriving(accessors)
type team = {
  id: Id.t,
  avoidIds: Id.Set.t,
  score: float,
}
// 自动生成 teamIdGet, teamAvoidIdsGet, teamScoreGet 等函数
```

---

## 第四章 · React 在 ReScript 中的实践

### 4.1 组件声明
```rescript
@react.component
let make = (~prop1: type1, ~prop2: type2) => {
  let (state, setState) = React.useState(() => initialValue)
  <div> {React.string("Hello")} </div>
}
```

### 4.2 Hooks 使用
```rescript
// useState
let (value, setValue) = React.useState(() => 0)

// useReducer（项目中大量使用）
let (state, dispatch) = React.useReducer(reducer, initialState)

// useEffect 依赖版本
React.useEffect0(() => { ...; None })           // 无依赖
React.useEffect1(() => { ...; Some(cleanup) }, [dep])  // 1 个依赖
React.useEffect2(() => { ...; None }, (a, b))   // 2 个依赖
React.useEffect3(() => { ...; None }, (a, b, c)) // 3 个依赖
```

### 4.3 条件渲染
```rescript
// if 表达式返回 React.null 表示不渲染
{if condition {
  <div> content </div>
} else {
  React.null
}}

// 更复杂的模式
switch data {
| Loaded => <Content />
| NotLoaded => <Loading />
| Error => <Error />
}
```

### 4.4 Children 模式
```rescript
// 组件接受 children
@react.component
let make = (~children, ~className="") =>
  <div className> children </div>

// render-prop 模式（项目核心）
<LoadTournament tourneyId windowDispatch>
  {data => <Inner data />}
</LoadTournament>
```

### 4.5 自定义 Hook 实战

**`useBool` — 用 useReducer 封装布尔状态**：
```rescript
type boolState = { state: bool, setTrue: unit => unit, setFalse: unit => unit }
let useBool = init => {
  let (state, setState) = React.useReducer(reducer, init)
  { state, setTrue: () => setState(true), setFalse: () => setState(false) }
}
```

**`useSortedTable` — 泛型排列表格**：
```rescript
// 参数化的 getter variant 模式
type getter<'a> = GetString('a => string) | GetInt('a => int) | ...
// reducer 中根据 variant 选择不同的比较函数
```

**`useLoadingCursorUntil` — 使用 %raw 的副作用 Hook**：
```rescript
let useLoadingCursorUntil = isLoaded =>
  React.useEffect1(() => {
    let _ = isLoaded ? %raw(`...cursor="auto"`) : %raw(`...cursor="wait"`)
    let reset = () => %raw(`...cursor="auto"`)
    Some(reset)  // cleanup
  }, [isLoaded])
```

---

## 第五章 · 前端存储 — IndexedDB + localforage

### 5.1 架构设计

```
                                             ┌─────────────┐
                                             │  Components │
                                             └──────┬──────┘
                                                    │ useAllPlayers/Teams/Tournaments
                                             ┌──────▼──────┐
                        Record.t (单条) ─────►│    Db.res   │◄──── Map.t (键值集合)
                        config / auth         └──────┬──────┘
                                                    │
                              ┌─────────────────────┼──────────────────────┐
                              ▼                     ▼                      ▼
                     ┌─────────────┐      ┌───────────────┐      ┌───────────────┐
                     │   Players   │      │     Teams      │      │  Tournaments   │
                     │   Store     │      │    Store       │      │    Store       │
                     └──────┬──────┘      └───────┬───────┘      └───────┬───────┘
                            │                     │                      │
                            └─────────────────────┼──────────────────────┘
                                                  ▼
                                          ┌───────────────┐
                                          │   localforage  │
                                          │   (IndexedDB)  │
                                          └───────────────┘
```

### 5.2 Functor 模式实现类型安全存储

```rescript
module type Encodable = {
  type t
  let encode: t => Js.Json.t
  let decode: Js.Json.t => t
}
```

每个数据模型（Player、Team、Tournament）提供自己的 `encode/decode`，Db 层通过 Functor 参数化接受，实现**零运行时开销的类型擦除**。

### 5.3 数据库 Hook 模式

```rescript
let useAllDb = store => {
  let (items, dispatch) = React.useReducer(genericDbReducer, Id.Map.make())
  let loaded = Hooks.useBool(false)
  
  // 加载：从 IndexedDB 拉取所有记录
  React.useEffect0(() => {
    Store.Map.getAllItems(store) ->|> dispatch(SetAll(...))
  })
  
  // 持久化：任何变更自动写回
  React.useEffect2(() => {
    if loaded.state { Store.Map.setItems(store, ~items) }
    None
  }, (items, loaded.state))
  
  {items, dispatch, loaded: loaded.state}
}
```

### 5.4 测试替换 — 接口隔离

```rescript
// 生产: LocalForage（真实 IndexedDB）
// 测试: TestStore（内存 Map + ref）
let store: module(STORE) = if isTest {
  module(TestStore)
} else {
  module(LocalForage)
}
```

---

## 第六章 · 路由与状态管理

### 6.1 路由系统
```rescript
type t =
  | Index | TournamentList | Tournament(Data.Id.t)
  | Players | Options | NotFound

// URL → Variant
let fromPath = x => switch x {
| list{} => Index
| list{"tourneys", x} => Tournament(id(x))
| _ => NotFound
}

// Variant → URL
let toString = x => switch x {
| Tournament(id) => "/tourneys/" ++ str(id)
| ...
}
```

### 6.2 Link 组件
```rescript
module Link = {
  @react.component
  let make = (~children, ~to_, ~onClick=?) => {
    let href = toString(to_)
    React.cloneElement(
      <a href onClick={e => RescriptReactRouter.push(href)}> children </a>,
      {"aria-current": isActive},
    )
  }
}
```

### 6.3 状态管理模式
- **全局状态**：`Db.useConfig()` / `Db.useAuth()` — 通过 `useReducer` + IndexedDB 持久化
- **页面状态**：各页面的 `useReducer` — 数据的 CRUD
- **赛事状态**：`LoadTournament` render-prop — 加载 tournament + 推导 roundCount / isNewRoundReady
- **无全局 store**：不使用 Redux/Zustand，靠 Context + props 传递

---

## 第七章 · 数据结构与算法

### 7.1 瑞士移位制配对引擎 (`Data_Pairing.res`)

**核心算法**：
1. **按积分排序分组** → 同分区组内再分上下半区
2. **构建加权完全二分图** → 每个队伍对之间计算优先级分数
3. **Blossom 算法求解最大权完美匹配** → `rescript-blossom`

**优先级函数**：
```
对局优先级 = 不同半区分(最高) + 场分相近(次高) + 未对阵过(更高)
halves = isDiffHalf ? 4/(halfPosDiff+1) : 0
scores = maxScore*16 - scoreDiff*16  
canMeet = canMeet ? 32*maxScore : 0
```

**轮空处理**：
- 奇数队伍时，从轮空队列中优先选最近没轮空过的
- 积分最低且未轮空过的队伍获得轮空权，自动得 1 分

### 7.2 积分系统 (`Data_Scoring.res`)

**场分规则**：
| 结果 | 分数 | 说明 |
|------|------|------|
| 胜 | 1.0 | 对方级数更低 |
| 平 | 0.5 | 双方同级数 |
| 负 | 0.0 | |
| 缺席 | -1.0 | 对手自动 1.0 |
| 轮空 | 1.0 | 自动获胜 |

**破同分（瑞士制）**：
```
总积分 → 对手分(Buchholz) → 胜场数 → 累积小分
```

**Buchholz 对手分计算**：
```rescript
// 第二遍遍历：每个队伍的对手分 = 所有对手的总场分之和
Id.Map.map(scores, ({opponentResults, _}) => {
  let opponentScore = List.reduce(opponentResults, 0., (acc, (oppId, _)) => {
    acc +. getTotalFieldScore(oppId)
  })
})
```

### 7.3 淘汰赛 Bracket 模板

```rescript
// 标准种子对阵模板
let template8 = [
  (1, 8, "上半区①"), (4, 5, "上半区②"),
  (3, 6, "下半区③"), (2, 7, "下半区④"),
]
// 第 i 个种子排在 template[i] 的对应位置
```

### 7.4 CSV 解析

```rescript
// 纯函数式 CSV 解析器
// 格式: 队伍名称,队员1,性别,队员2,性别,俱乐部,积分
let parseLine = (line: string): option<row> => { ... }
let parse = (text: string): array<row> => 
  text |> stripBOM |> split("\n") |> filter(nonEmpty) |> map(parseLine) |> keepSome
```

---

## 第八章 · 工具函数与设计模式

### 8.1 泛型比较函数

```rescript
type direction<'data, 'field> = (
  ('field, 'field) => int,  // 比较函数
  'data => 'field,          // getter
  'data, 'data              // 两个数据
) => int

// 升序工厂
let ascend = (cmp, getter, a, b) => cmp(getter(a), getter(b))
// 降序工厂（参数反转）
let descend = (cmp, getter, a, b) => cmp(getter(b), getter(a))
```

### 8.2 Immutable 数组操作

```rescript
module Array = {
  let swap = (arr, idx1, idx2) => {
    switch (arr[idx1], arr[idx2]) {
    | (Some(item1), Some(item2)) =>
      Array.setUnsafe(arr, idx1, item2)
      Array.setUnsafe(arr, idx2, item1)
      arr
    | (None, _) | (_, None) => arr
    }
  }
}
```

### 8.3 表单验证 (re-formality 模式)

```rescript
type fieldStatus<'outputValue> =
  | Pristine                // 未触碰
  | Dirty(result<'outputValue, string>)  // 已验证

// Validator 模式：输入 → Result(输出, 错误消息)
let validateFieldOnBlurWithValidator = (~input, ~fieldStatus, ~validator) => ...
```

### 8.4 静态资源管理

```rescript
module WebpackAssets = {
  @module("../graphics/AotearoaGuandanLogo.png") 
  external logo: string = "default"
}
// Vite 自动处理 hash / 路径
```

---

## 第九章 · Python 辅助工具

### 9.1 文件清单 (`pytools/`)

| 文件 | 功能 |
|------|------|
| `haixuansai.py` | 海选赛瑞士制配对参考实现 |
| `simulate_guandan.py` | 完整赛事模拟器（瑞士→小组→淘汰） |
| `requirements.txt` | 依赖 |
| `*.py` | 其他辅助脚本 |

### 9.2 关注要点
- Python 实现的配对算法与 ReScript 实现的对齐方式
- 模拟器如何调用配对逻辑批量生成结果 → 用于输出 `output/*.csv` 测试数据
- 两种语言的算法实现对比（命令式 vs 函数式）

---

## 第十章 · 测试体系

### 10.1 技术栈
- **Vitest 3** + **jsdom**（浏览器环境模拟）
- **@testing-library/react** + **@testing-library/dom**
- **rescript-vitest**（ReScript 测试绑定）

### 10.2 测试结构
```
tests/
  ├── setup.js                  # 全局 setup
  ├── *_test.res                # ReScript 测试文件
  └── *_test.res.mjs            # 编译产物
testutils/
  └── *.res                     # 测试辅助函数
```

### 10.3 测试模式
- **隔离数据**：TestStore 替换真实 IndexedDB
- **端到端**：从数据创建到配对算法验证
- **快照**：导出 CSV 用于回归测试

---

## 第十一章 · 构建与部署

### 11.1 开发流程
```bash
pnpm run res:dev     # ReScript 编译监听
pnpm run dev         # Vite 开发服务器（端口 3000）
pnpm run test        # 运行测试
pnpm run res:clean   # 清理编译产物
pnpm run format      # 格式化所有 .res 文件
```

### 11.2 生产构建
```bash
pnpm run build       # ReScript 编译 + Vite 打包 → dist/
```

### 11.3 特殊配置
- `base: "./"` — 相对路径，支持 `file://` 协议直接打开
- 自定义 vite 插件 `remove-crossorigin` — 移除 `<script>` 的 crossorigin 属性
- `__LAST_COMMIT_DATE__` — 编译时注入 git 信息

### 11.4 部署
- **Netlify**：SPA 回退路由（所有路径 → index.html）
- 部署地址：`https://ai3d.co.nz/guandan`

---

## 第十二章 · 建议学习路径

### 阶段一：理解语言（3-5 天）
1. 阅读 [ReScript 官方文档](https://rescript-lang.org/docs/manual)
2. 重点掌握：Variant、Pattern Matching、Option、Pipe、Record
3. 做到能读懂项目中任意一个 `.res` 文件

### 阶段二：理解架构（3-5 天）
1. 从 `Main.res` → `App.res` → `Db.res` 追踪启动流程
2. 理解 render-prop 模式：`LoadTournament` 如何加载赛事数据
3. 理解数据流：`Db.res useAllDb → LoadTournament → PageTourney → PageRound`

### 阶段三：算法深挖（5-7 天）
1. **`Data_Scoring.res`**：积分计算、对手分、破同分比较器
2. **`Data_Pairing.res`**：瑞士制配对引擎、Blossom 算法应用
3. **`Data_Knockout.res`**：种子排位、bracket 模板
4. **`Data_GroupStage.res`**：分组轮转算法

### 阶段四：动手实践（7+ 天）
1. 尝试增加一种新赛制（如双败淘汰制）
2. 修改积分规则并验证所有测试
3. 添加一个新页面的完整流程（模型 → 存储 → UI → 路由）
4. 修复一个 bug，完成完整的开发→编译→测试循环

---

## 附录 · 技术要点速查表

### A. 文件命名规范
| 模式 | 含义 | 示例 |
|------|------|------|
| `Data_Xxx.res` | 数据模型 | `Data_Team.res` |
| `PageXxx.res` | 页面组件 | `PagePlayers.res` |
| `PageTournament/Xxx.res` | 赛事子页面 | `PageRound.res` |
| `Xxx.resi` | 接口文件（隐藏实现） | `Data_Rounds.resi` |
| `Xxx.res` + `Xxx.mjs` | ReScript 源码 + 编译产物 | 成对出现 |

### B. 核心依赖速查
| npm 包 | 用途 |
|--------|------|
| `@rescript/core` | 标准库（Array, Belt, String 等） |
| `@rescript/react` | React 绑定 |
| `@ryyppy/rescript-promise` | Promise 绑定 |
| `rescript-webapi` | Web API 绑定（DOM, window 等） |
| `rescript-blossom` | 图匹配算法 |
| `localforage` | IndexedDB 封装 |
| `nanoid` | 生成唯一 ID |
| `react-feather` | 图标库 |
| `@reach/dialog` | 无障碍对话框 |
| `@octokit/core` | GitHub API（云同步） |

### C. ReScript 编译器配置要点
- `jsx: {version: 4}` — 使用新版 JSX 编译
- `in-source: true` — 编译产物放在源码旁边（`.res.mjs`）
- `namespace: true` — 生成命名空间
- `-open RescriptCore` — 全局打开标准库
- `+A-3-44-102` — 将未使用变量、不必要的 `Some` 等作为**错误**

### D. 已修复的真实 Bug（从本次学习会话）

**Bug 1：瑞士制添加队伍后没有排位按钮**
- 根因：`"+ 开始新一轮"` 按钮的显示条件需要 `isCurrentTabRound`，但初始 tab 是 Status
- 修复：去掉 tab 限制，`isNewRoundReady` 为 true 即显示

**Bug 2：平局导致下一轮无法开始**
- 根因：`isRoundComplete` 将平局的 `winner=None` 视为"未录入"
- 修复：增加 `isMatchScored` 函数区分"未录入"与"已录入平局"

**Bug 3：after: 删除无用变量**
- 移除未使用的 `isCurrentTabRound`
